
    
  <div class="col d-grid gap-2 d-md-flex justify-content-md-end align-self-center">
      <p>para <a href="/logout">cerrar sesión</a></p>
    </div>
 
<div class="text-center row text-center mt-2">
  <div class="bg-dark text-light"><h2>
    <b>Bienvenido Administrador</b>
  </h2>
</div>
<hr>
<div>
  <hr>
</div>
  </div>
</div>
<div class="col">
  <table class='table table-striped table-hover'>
    <thead>
      <tr>
        <th href="/admin/{{id}}" value="DELETE" scope='col' id="id">id</th>
        <th scope='col'>Nombre</th>
        <th scope='col'>Email</th>
        <th scope='col'>Rut</th>
        <th scope='col'>Dirección</th>
        <th scope='col'>Saldo</th>
      </tr>
    </thead>
    {{#each allUsers}}
    <tbody>
      <tr>
        <th scope='row' href="/admin/{{id}}" value="DELETE" id="id">{{this.id}}</th>
        <td>{{this.name}}</td>
        <td>{{this.email}}</td>
        <td>{{this.rut}}</td>
        <td>{{this.address}}</td>
        <td>${{this.balance}}</td>

        <td><button type="submit" class="btn btn-danger" onclick="borrar({{id}})" value="DELETE">Eliminar</button></td>

      </tr>
    </tbody>
    {{/each}}
</div>
</div>

<script>
  function borrar(id) {
    axios.delete("http://localhost:3000/admin/" + id).then(() => {
      alert("Cliente id" + id + " eliminado");

    });
  }
</script>
<hr>
 